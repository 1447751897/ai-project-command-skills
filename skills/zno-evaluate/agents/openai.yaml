interface:
  display_name: "zno-evaluate"
  short_description: "Validate whether a project idea is worth doing"
  default_prompt: "Use $zno-evaluate with /zno-evaluate to challenge a project idea before committing to building it."
