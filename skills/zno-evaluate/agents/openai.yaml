interface:
  display_name: "Evaluate"
  short_description: "Validate whether a project idea is worth doing"
  default_prompt: "Use $evaluate with /zno-evaluate to challenge a project idea before committing to building it."
