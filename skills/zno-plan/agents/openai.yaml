interface:
  display_name: "zno-plan"
  short_description: "Recommend next features and priorities"
  default_prompt: "Use $zno-plan with /zno-plan to recommend next features, improvements, and priorities before implementation."
