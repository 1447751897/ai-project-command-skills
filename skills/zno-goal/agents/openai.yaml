interface:
  display_name: "Goal"
  short_description: "Auto-progress a full project objective"
  default_prompt: "Use $goal with /zno-goal or /zno-goal --super to automatically progress a project objective."
