interface:
  display_name: "zno-super"
  short_description: "Alias for /zno-goal --super"
  default_prompt: "Use $zno-super as an alias for /zno-goal --super to autonomously progress a project objective."
