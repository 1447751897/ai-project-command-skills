# Project Structure

本文档用于说明项目整体框架、目录职责、关键文件作用和模块边界。任何新开发者或 AI 助手介入项目时，都应先阅读本文档。

## 1. 项目结构总览

```text
project-root/
  path/
    file-or-folder
```

## 2. 顶层目录说明

| 路径 | 类型 | 作用 | 维护注意 |
| --- | --- | --- | --- |
|  | 目录/文件 |  |  |

## 3. 前端结构

| 路径 | 作用 | 典型文件 | 说明 |
| --- | --- | --- | --- |
|  |  |  |  |

## 4. 后端结构

| 路径 | 作用 | 典型文件 | 说明 |
| --- | --- | --- | --- |
|  |  |  |  |

## 5. 配置与环境

| 文件 | 作用 | 是否可改 | 注意事项 |
| --- | --- | --- | --- |
|  |  | 是/否 |  |

## 6. 核心模块地图

| 模块 | 入口文件 | 依赖模块 | 对外提供能力 | 维护注意 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 7. 数据流 / 请求流

```text
用户操作 ->
前端页面/组件 ->
状态管理/请求封装 ->
后端接口 ->
服务层 ->
数据存储 ->
返回结果
```

## 8. 新增文件放置规则

```text
新增页面：
新增组件：
新增 API：
新增服务：
新增类型：
新增测试：
新增配置：
```

## 9. 不建议改动区域

| 路径 | 原因 | 改动前需要确认 |
| --- | --- | --- |
|  |  |  |

## 10. 维护要求

当出现以下情况时，必须更新本文档：

1. 新增或删除顶层目录。
2. 新增核心模块。
3. 调整前端、后端、数据、配置目录结构。
4. 引入新的框架、构建方式或部署结构。
5. 发现原有目录说明与实际代码不一致。
